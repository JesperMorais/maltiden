---
phase: 07-architecture-deployment-polish
plan: 01
type: execute
---

<objective>
Introduce storage interfaces for testability, extract DI wiring, and fix remaining architecture inconsistencies.

Purpose: Enable mock-based unit testing and future database migration by decoupling services from concrete storage types.
Output: Interface definitions, updated service constructors, extracted dependency wiring, consistent context helpers.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/phase-prompt.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/FIX-PLAN.md

# Prior phase summaries (dependency graph):
@.planning/phases/05-error-handling-input-validation/05-01-SUMMARY.md
@.planning/phases/06-performance-database/06-02-SUMMARY.md

# Key source files:
@backend/internal/api/router.go
@backend/internal/storage/sqlite/user_storage.go
@backend/internal/storage/sqlite/household_storage.go
@backend/internal/storage/sqlite/recipe_storage.go
@backend/internal/storage/sqlite/menu_storage.go
@backend/internal/storage/sqlite/shopping_storage.go
@backend/internal/services/auth_service.go
@backend/internal/services/household_service.go
@backend/internal/services/recipe_service.go
@backend/internal/services/menu_service.go
@backend/internal/services/shopping_service.go
@backend/pkg/middleware/auth.go
@backend/internal/api/handlers/offers.go

**FIX-PLAN Batch 7 findings:** ARCH-01, ARCH-02, ARCH-03, ARCH-04, ARCH-05, ARCH-08, ARCH-09, ARCH-13

**Tech stack available:** Go 1.24, SQLite, stdlib net/http
**Established patterns:** Sentinel errors (domain/errors.go), WriteError/WriteJSON helpers (handlers/response.go), context timeouts on all storage methods
**Constraining decisions:**
- Phase 5: Sentinel errors as package-level vars in domain/errors.go
- Phase 6: Context created internally in storage methods (handlers don't pass context yet)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define storage interfaces and update service constructors</name>
  <files>backend/internal/domain/repositories.go, backend/internal/services/auth_service.go, backend/internal/services/household_service.go, backend/internal/services/recipe_service.go, backend/internal/services/menu_service.go, backend/internal/services/shopping_service.go</files>
  <action>
Create `backend/internal/domain/repositories.go` with minimal interfaces — only methods currently called by services. Read each storage file to extract the exact method signatures used.

Interfaces needed:
- `UserRepository`: GetByEmail, GetByID, Create, CreateTx (if exists)
- `HouseholdRepository`: Create, CreateTx, GetByID, GetInvite, CreateInvite, JoinHousehold, GetMembers, GetMemberStatuses, UpdateMemberStatus, RemoveMember
- `RecipeRepository`: GetAll, GetAllPaginated, GetByID, GetByIDs, Create, GetByTags
- `MenuRepository`: Create, GetCurrent, GetMenuForHousehold (IDOR check method)
- `ShoppingRepository`: GetByMenuID, UpdateItem

Place interfaces in `backend/internal/domain/repositories.go` — domain package is already the leaf dependency used by all layers.

Then update ALL service struct fields and constructors to accept interfaces instead of concrete `*sqlite.XxxStorage` types:
- `AuthService`: Change `userStorage *sqlite.UserStorage` → `userStorage domain.UserRepository` (also needs `db *sql.DB` for transactions — keep this for now)
- `HouseholdService`: Change both storage fields to interfaces
- `RecipeService`: Change to interface
- `MenuService`: Change both storage fields to interfaces
- `ShoppingService`: Change all three storage fields to interfaces

Remove `"maltiden/internal/storage/sqlite"` imports from all service files (they should only import `"maltiden/internal/domain"`).

Verify: `cd backend && go build ./...` compiles without errors (SQLite concrete types already implement these interfaces implicitly via Go structural typing).
  </action>
  <verify>cd backend && go build ./... && go vet ./...</verify>
  <done>All 5 storage interfaces defined. All 6 service constructors accept interfaces. No service file imports sqlite package. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Extract DI wiring, fix context helpers, and deduplicate coordinate parsing</name>
  <files>backend/internal/api/router.go, backend/pkg/middleware/auth.go, backend/internal/api/handlers/offers.go, backend/internal/api/handlers/shopping.go</files>
  <action>
Three focused changes:

**1. Extract wireDependencies in router.go (ARCH-03):**
Move lines 16-36 (storage→service→handler creation) into a helper function. Keep route registration in NewRouter. The helper should return a struct or just create the handlers — keep it simple, no DI container needed. Something like:

```go
type deps struct {
    auth      *handlers.AuthHandler
    household *handlers.HouseholdHandler
    recipe    *handlers.RecipeHandler
    menu      *handlers.MenuHandler
    shopping  *handlers.ShoppingHandler
    offers    *handlers.OffersHandler
}

func wireDependencies(db *sql.DB) *deps { ... }
```

The ShoppingHandler constructor currently takes `menuStorage` directly for IDOR checks — update it to accept `domain.MenuRepository` interface instead.

**2. Fix GetHouseholdID signature (ARCH-02, ARCH-04):**
In `backend/pkg/middleware/auth.go`, change `GetHouseholdID(ctx context.Context) string` to `GetHouseholdID(r *http.Request) string` to match `GetUserID(r *http.Request) string`. Then update ALL callers that pass `r.Context()` to pass `r` instead. Search for `middleware.GetHouseholdID(` across the codebase to find all call sites.

**3. Deduplicate coordinate parsing (ARCH-13):**
In `backend/internal/api/handlers/offers.go`, the lat/lng/radius parsing is duplicated across SearchOffers, GetDiscounts, and GetStores. Extract to a private helper:

```go
func parseLocationParams(r *http.Request) (lat, lng, radius float64, err error) { ... }
```

Also extract `parseExcludeStores(r *http.Request) []string` if duplicated.

Do NOT move domain types to a separate api/types package (ARCH-05) — that's unnecessary churn for quick depth and the domain package works fine for this project's size.
  </action>
  <verify>cd backend && go build ./... && go vet ./...</verify>
  <done>Router uses wireDependencies() for DI. GetHouseholdID accepts *http.Request (consistent with GetUserID). Coordinate parsing extracted to helper. Build passes.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd backend && go build ./...` succeeds
- [ ] `cd backend && go vet ./...` no warnings
- [ ] `cd backend && go test ./...` passes (existing household service tests still work)
- [ ] No service file imports `"maltiden/internal/storage/sqlite"`
- [ ] domain/repositories.go has 5 interfaces
- [ ] router.go uses wireDependencies()
- [ ] GetHouseholdID signature matches GetUserID (both accept *http.Request)
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Services decoupled from concrete storage types
- No regressions in existing tests
</success_criteria>

<output>
After completion, create `.planning/phases/07-architecture-deployment-polish/07-01-SUMMARY.md`
</output>
